/* replace the top "Comments" link on works with "Hide Comments" */
$("#show_comments_link_top").html("<%= escape_javascript(show_hide_comments_link(@commentable, :link_type => "hide")) %>");
/* replace the top "Comments (#)" link with "Hide Comments (#)" */
$("#show_comments_link").html("<%= escape_javascript(show_hide_comments_link(@commentable, :link_type => "hide", :show_count => true)) %>");
/* load up the comments in their placeholder div */
$("#comments_placeholder").html("<%= escape_javascript(will_paginate(@comments, {:remote => true})) %>");
$("#comments_placeholder").append("<%= escape_javascript(render "comments/comment_thread", {:comments => @comments, :depth => 0}) %>");
$("#comments_placeholder").append("</ol>");
$("#comments_placeholder").append("<%= escape_javascript(will_paginate(@comments, {:remote => true})) %>");
/* roll down the comments */
$("#comments_placeholder").slideDown();
