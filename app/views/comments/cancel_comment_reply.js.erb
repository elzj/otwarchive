/* unhide the original navigation */
$("#navigation_for_comment_<%= "#{@commentable.id}" %>").show();
/* get rid of the comment form */
$("#add_comment_reply_placeholder_<%= "#{@commentable.id}" %>").slideUp();
$("#add_comment_reply_placeholder_<%= "#{@commentable.id}" %>").html("");
