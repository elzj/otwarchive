/* hide, then remove the delete confirmation form */
$("#delete_comment_placeholder_<%= "#{@comment.id}" %>").slideUp();
$("#delete_comment_placeholder_<%= "#{@comment.id}" %>").html("");
/* unhide the navigation */
$("#navigation_for_comment_<%= "#{@comment.id}" %>").show();
